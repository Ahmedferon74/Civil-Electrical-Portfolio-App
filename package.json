{
  "name": "civil-electrical-app",
  "version": "1.0.0",
  "description": "Civil-Electrical Portfolio App for Vercel",
  "author": "Ahmed Feron",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install-all": "npm install && cd frontend && npm install",
    "start": "cd frontend && npm run preview"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}

